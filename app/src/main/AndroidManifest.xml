<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naumov.illia.illianaumov">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".main.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.mvp.view.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.mvp.view.TransitionImageSecondActivity"
            android:parentActivityName=".main.mvp.view.TransitionImageFirstActivity" />
        <activity
            android:name=".main.mvp.view.TransitionImageFirstActivity"
            android:parentActivityName=".main.mvp.view.MainActivity" />
        <activity android:name=".main.mvp.view.CurrencyActivity" android:parentActivityName=".main.mvp.view.MainActivity"/>
    </application>

</manifest>